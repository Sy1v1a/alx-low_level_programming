#include <main.h>
/**
 * _memset.c - fills memeory with a constant byte
 * @s: memory to be filled
 * @b: constant byte
 * @n: bytes to be changed
 * return: new value for n bytes
 */
char *_memset(char *s, char b, unsigned int n){
int d = 0;

return (s);
}
