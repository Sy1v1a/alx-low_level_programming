#include <main.h>
/**
 * _memcpy - copies memory area
 * @dest: where to store memory
 * @src: where to copy momory
 * @n: number of bytes
 * return: n memory bytes and copied memory
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
int i = 0;

return (dest);
}
