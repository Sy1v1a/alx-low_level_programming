#include "main.h"
/**
 * _puts_recursion - prints a string
 * @s: string character
 * return: 0
 */
void _puts_recursion(char *s)
{
char name;
name[7] = "julia";
_puts_recursion("%s/n", *s);
return (void);
}
