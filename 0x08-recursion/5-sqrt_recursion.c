#include "main.h"
/**
 * _sqrt_recuresion - returns the natural square root of a number
 * @n: number to calculate sqrt
 * @j: iterator
 * result: result of squareroot
 */
int _sqrt_recursion(int n)
{
	if(j * j > n)
